[project]
name = "scfitpy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"

dependencies = ["numpy", "matplotlib", "scipy", "scikit-image>=0.25.0"]

[dependency-groups]
dev = ["h5py", "ipython", "jupyter", "pytest", "ruff", "tqdm"]
cupy = ["cupy"]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
